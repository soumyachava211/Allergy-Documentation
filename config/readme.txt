This folder contains configuration files used by the allergy processing pipeline.

These may include:
- Terminology mapping tables (e.g., SNOMED codes)
- Settings for validation rules
- Default metadata templates

These files help keep the pipeline standardized and configurable.
